#define MAX_CORR    256
//ber is the bulk of data to extract
//blen is length of ber
//corr is key of hash
int extract_correlate (char *ber, int blen, char *corr);
//int messages_correlate(char *buff_num, int buff_len);
